pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build correcto'
            }
        }

        stage('Test') {
            steps {
                echo 'Test correcto'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy con Docker'
            }
        }
    }
}